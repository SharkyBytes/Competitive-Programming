#include<bits/stdc++.h>

using namespace std;

void solve() {
    long long a,b,c;
    cin >> a>>b>>c;
    // int t;
    // cin>>t;
    // vector<int> arr(n);
    // for (int i = 0; i < n; i++) 
    //     cin >> arr[i];

long long n=(a+2*b+3*c);
cout<<n%2<<endl;


   
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}